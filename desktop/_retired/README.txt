EmulatorLocator.cs.txt - RETIRED 2026-09-21, not compiled.

Why: the owner's call that day - "I do not want to run the game without the emulator. The app
will take an iso and return an iso. This iso will be able to be used with an emulator."

That makes the app's contract strictly ISO in -> ISO out. Finding PCSX2 and launching it was
scope nobody asked for, so the feature was pulled rather than left in. Kept here because the
detection list is useful if a future request wants an "Open in emulator" convenience.
